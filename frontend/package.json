{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "scripts": {
    "dev": "concurrently \"npm --prefix ./buyer-portal run dev\" \"npm --prefix ./main-site run dev\" \"npm --prefix ./seller-portal run dev\" \"npm --prefix ./retail-portal run dev\" \"npm --prefix ./admin-portal run dev\" \"npm --prefix ./delivery-portal run dev\" --names \"buyer,main,seller,retail,admin,delivery\" --prefix-colors \"bgBlue.bold,bgGreen.bold,bgYellow.bold,bgMagenta.bold,bgCyan.bold,bgRed.bold\""
  }
}
